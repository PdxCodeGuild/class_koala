#averageCalc
num = []

i = input("enter a number or 'done'\n")
while i != "done":
    num.append(i)
    i = input("enter a number or 'done'\n")


for i in range(len(num)):
    num[i] = float(num[i])

total = sum(num)
avg = total/len(num)

print(f"\nthe average is {avg}")
